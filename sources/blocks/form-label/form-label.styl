.form-label
  display block
  position relative
  .text-field
    display block
    width 100%

  &__text
    color #9e9e9e
    line-height rem(65px)
    transition $transition-base
    pointer-events none
    position absolute
    top 0
    left unquote('calc(' + rem(18px) + ' + 2px)')
